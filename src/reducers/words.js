import {} from '../actions/words';

const initialState = {
  error: null
};

export default function reducer (state = initialState, action) {
  // if (action.type === ACTION_NAME) {
  //   // Do something;
  // }
  return state;
}
